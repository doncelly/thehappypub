// Placeholder — reemplazar corriendo, después de aplicar supabase/schema.sql:
//
//   npm run supabase:types
//
// (requiere `supabase login` y NEXT_PUBLIC_SUPABASE_PROJECT_ID en .env.local)
//
// Mientras tanto, Database = any evita que los clientes tipados rompan el build.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
